{
  "services": [
    {
        "id": "nginx",
        "name": "nginx",
        "tags": ["http"],
        "port": 80,
        "checks": [{
            "script": "echo '# curl -sI http://localhost:80' ; curl -sI http://localhost:80",
            "interval": "10s"
        }]
    }
  ]
}
