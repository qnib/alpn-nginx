{
  "services": [
    {
        "id": "nginx-ssl",
        "name": "nginx-ssl",
        "tags": ["https"],
        "port": 443,
        "checks": [{
            "script": "echo '# curl -sI -k https://localhost:443' ; curl -sI -k https://localhost:443",
            "interval": "10s"
        }]
    }
    
  ]
}
